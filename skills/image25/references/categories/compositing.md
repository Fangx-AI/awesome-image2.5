# 合成与风格转换 · Compositing & style

[返回总览](../gallery.md)

所有条目均未经过指定模型出图验证。以“变体”标记的条目共享同一基础场景。

## 商品置入场景

ID: `compositing-product-scene` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene.txt)

检查：参考图角色；透视；光照；遮挡。

## 商品置入场景 · 边缘精修

ID: `compositing-product-scene-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene-edge.txt)

检查：参考图角色；透视；光照；遮挡。

## 商品置入场景 · 光影衔接

ID: `compositing-product-scene-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene-light.txt)

检查：参考图角色；透视；光照；遮挡。

## 商品置入场景 · 纹理衔接

ID: `compositing-product-scene-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene-texture.txt)

检查：参考图角色；透视；光照；遮挡。

## 商品置入场景 · 透视对齐

ID: `compositing-product-scene-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene-perspective.txt)

检查：参考图角色；透视；光照；遮挡。

## 商品置入场景 · 细节保留

ID: `compositing-product-scene-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene-identity.txt)

检查：参考图角色；透视；光照；遮挡。

## 商品置入场景 · 色彩匹配

ID: `compositing-product-scene-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene-color.txt)

检查：参考图角色；透视；光照；遮挡。

## 商品置入场景 · 文字保护

ID: `compositing-product-scene-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene-text.txt)

检查：参考图角色；透视；光照；遮挡。

## 商品置入场景 · 最小改动

ID: `compositing-product-scene-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene-minimal.txt)

检查：参考图角色；透视；光照；遮挡。

## 商品置入场景 · 成品审阅

ID: `compositing-product-scene-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是目标桌面场景，图2是待放入的商品参考。
构图与视觉：将图2商品放在图1空桌面上，匹配尺度、透视、反射和接触阴影。
必须遵守：保留商品形状与标签，不修改图1现有物体。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-product-scene-final.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换

ID: `compositing-style-transfer` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换 · 边缘精修

ID: `compositing-style-transfer-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer-edge.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换 · 光影衔接

ID: `compositing-style-transfer-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer-light.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换 · 纹理衔接

ID: `compositing-style-transfer-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer-texture.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换 · 透视对齐

ID: `compositing-style-transfer-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer-perspective.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换 · 细节保留

ID: `compositing-style-transfer-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer-identity.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换 · 色彩匹配

ID: `compositing-style-transfer-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer-color.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换 · 文字保护

ID: `compositing-style-transfer-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer-text.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换 · 最小改动

ID: `compositing-style-transfer-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer-minimal.txt)

检查：参考图角色；透视；光照；遮挡。

## 风格参考转换 · 成品审阅

ID: `compositing-style-transfer-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是内容目标，图2只作为色彩与笔触参考。
构图与视觉：保留图1构图和主体，借用图2的色板、边缘处理和纹理密度。
必须遵守：不把图2的具体人物或物件带入结果。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-style-transfer-final.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染

ID: `compositing-sketch-render` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染 · 边缘精修

ID: `compositing-sketch-render-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render-edge.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染 · 光影衔接

ID: `compositing-sketch-render-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render-light.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染 · 纹理衔接

ID: `compositing-sketch-render-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render-texture.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染 · 透视对齐

ID: `compositing-sketch-render-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render-perspective.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染 · 细节保留

ID: `compositing-sketch-render-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render-identity.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染 · 色彩匹配

ID: `compositing-sketch-render-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render-color.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染 · 文字保护

ID: `compositing-sketch-render-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render-text.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染 · 最小改动

ID: `compositing-sketch-render-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render-minimal.txt)

检查：参考图角色；透视；光照；遮挡。

## 草图渲染 · 成品审阅

ID: `compositing-sketch-render-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是建筑草图，把它转为自然光下的建筑概念渲染。
构图与视觉：严格依据草图的体量、门窗位置与相机角度。
必须遵守：不擅自新增楼层或改变开口，不作为施工依据。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-sketch-render-final.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景

ID: `compositing-day-to-night` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景 · 边缘精修

ID: `compositing-day-to-night-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night-edge.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景 · 光影衔接

ID: `compositing-day-to-night-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night-light.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景 · 纹理衔接

ID: `compositing-day-to-night-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night-texture.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景 · 透视对齐

ID: `compositing-day-to-night-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night-perspective.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景 · 细节保留

ID: `compositing-day-to-night-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night-identity.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景 · 色彩匹配

ID: `compositing-day-to-night-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night-color.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景 · 文字保护

ID: `compositing-day-to-night-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night-text.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景 · 最小改动

ID: `compositing-day-to-night-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night-minimal.txt)

检查：参考图角色；透视；光照；遮挡。

## 日景转夜景 · 成品审阅

ID: `compositing-day-to-night-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把场景转换为傍晚蓝调时刻。
构图与视觉：窗户有少量暖光，环境整体变暗，阴影和天空一致。
必须遵守：建筑、人物、标志、物体数量与裁切不变。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-day-to-night-final.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换

ID: `compositing-season-change` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换 · 边缘精修

ID: `compositing-season-change-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change-edge.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换 · 光影衔接

ID: `compositing-season-change-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change-light.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换 · 纹理衔接

ID: `compositing-season-change-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change-texture.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换 · 透视对齐

ID: `compositing-season-change-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change-perspective.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换 · 细节保留

ID: `compositing-season-change-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change-identity.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换 · 色彩匹配

ID: `compositing-season-change-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change-color.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换 · 文字保护

ID: `compositing-season-change-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change-text.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换 · 最小改动

ID: `compositing-season-change-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change-minimal.txt)

检查：参考图角色；透视；光照；遮挡。

## 季节转换 · 成品审阅

ID: `compositing-season-change-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把夏季庭院转换为初秋。
构图与视觉：树叶增加黄橙色，地面少量落叶，保留自然光方向。
必须遵守：保留建筑结构、道路和家具位置，不添加无关物件。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：参考图角色、透视、光照、遮挡。检查非修改区域是否保持一致。
```

[下载文本](../prompts/compositing-season-change-final.txt)

检查：参考图角色；透视；光照；遮挡。

## 多图物件合成

ID: `reference-composite` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：2 张（最低） · 状态：`prompt-only`

```text
Use image 1 as the target scene and image 2 as the reference for the object to insert. Place the main object from image 2 on the empty surface in image 1. Preserve its shape and material while matching the target scene's camera perspective, scale, lighting direction and contact shadow. Keep all existing objects and the original crop from image 1 unchanged.
```

[下载文本](../prompts/reference-composite.txt)

检查：主体完整；文字准确；构图清晰；编辑保真。
