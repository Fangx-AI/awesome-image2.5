# UI与数字产品 · UI & digital products

[返回总览](../gallery.md)

所有条目均未经过指定模型出图验证。以“变体”标记的条目共享同一基础场景。

## 数据面板概念

ID: `ui-dashboard` · 基础案例 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 数据面板概念 · 编辑精选

ID: `ui-dashboard-editorial` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
版本要求：用于编辑精选展示。保留主要内容和媒介，删减非必要的背景装饰，让主视觉与次要细节的层级更鲜明。不要删除任务要求的文字、对象或图解关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard-editorial.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 数据面板概念 · 缩略图优先

ID: `ui-dashboard-thumbnail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
版本要求：用于缩略图浏览。通过更清晰的轮廓、主体与背景明暗分离来提升识别度；弱化非必要微小纹理，保留所有必要文字和完整主体。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard-thumbnail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 数据面板概念 · 细节呈现

ID: `ui-dashboard-detail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
版本要求：用于近距离查看。提升材质边缘、结构交界和局部细节的可辨性；沿用原构图和对象数量，不通过堆叠装饰来增加细节。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard-detail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 数据面板概念 · 克制留白

ID: `ui-dashboard-quiet` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
版本要求：用于安静、克制的视觉呈现。减少背景纹理和非必要装饰，强化负空间；保留必要的标签、关系、物件与原有视觉媒介。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard-quiet.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 数据面板概念 · 演示使用

ID: `ui-dashboard-presentation` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
版本要求：用于演示页面。确保远距离观看时主体层次仍然明确，必要文字清楚，信息区域之间有充足间距；不添加新的标题、数据或图例。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard-presentation.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 数据面板概念 · 印刷审阅

ID: `ui-dashboard-print` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
版本要求：用于印刷前视觉审阅。避免暗部完全糊成一片与高光大面积纯白溢出，保留边缘和细节；保持既有色板。本图仍是栅格概念，不声称已满足印刷色彩或出血规格。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard-print.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 数据面板概念 · 清晰辨识

ID: `ui-dashboard-accessible` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
版本要求：用于清晰辨识的设计版本。相邻重要元素除颜色差异外，还需有轮廓或明度差异；必要文字与底色对比充分，不凭色彩单独编码语义。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard-accessible.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 数据面板概念 · 均衡层次

ID: `ui-dashboard-balanced` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
版本要求：用于均衡层次的呈现。控制前中后景或主次信息的视觉权重，避免边缘装饰抢占注意力，保留全部关键对象、文字与关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard-balanced.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 数据面板概念 · 干净收尾

ID: `ui-dashboard-clean` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构项目管理仪表盘，左侧导航、上方三个摘要卡、下方任务列表。
构图与视觉：桌面界面正视图，白底与深蓝点缀，严格网格。
必须遵守：只使用演示数据，标题“Overview”，不能冒充真实业务结果。
版本要求：用于清理视觉瑕疵后的版本。重点消除不必要的重影、断裂边缘、融合物件和伪文字；不改变指定内容，不新增解释性元素。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-dashboard-clean.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页

ID: `ui-onboarding` · 基础案例 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页 · 编辑精选

ID: `ui-onboarding-editorial` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
版本要求：用于编辑精选展示。保留主要内容和媒介，删减非必要的背景装饰，让主视觉与次要细节的层级更鲜明。不要删除任务要求的文字、对象或图解关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding-editorial.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页 · 缩略图优先

ID: `ui-onboarding-thumbnail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
版本要求：用于缩略图浏览。通过更清晰的轮廓、主体与背景明暗分离来提升识别度；弱化非必要微小纹理，保留所有必要文字和完整主体。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding-thumbnail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页 · 细节呈现

ID: `ui-onboarding-detail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
版本要求：用于近距离查看。提升材质边缘、结构交界和局部细节的可辨性；沿用原构图和对象数量，不通过堆叠装饰来增加细节。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding-detail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页 · 克制留白

ID: `ui-onboarding-quiet` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
版本要求：用于安静、克制的视觉呈现。减少背景纹理和非必要装饰，强化负空间；保留必要的标签、关系、物件与原有视觉媒介。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding-quiet.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页 · 演示使用

ID: `ui-onboarding-presentation` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
版本要求：用于演示页面。确保远距离观看时主体层次仍然明确，必要文字清楚，信息区域之间有充足间距；不添加新的标题、数据或图例。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding-presentation.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页 · 印刷审阅

ID: `ui-onboarding-print` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
版本要求：用于印刷前视觉审阅。避免暗部完全糊成一片与高光大面积纯白溢出，保留边缘和细节；保持既有色板。本图仍是栅格概念，不声称已满足印刷色彩或出血规格。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding-print.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页 · 清晰辨识

ID: `ui-onboarding-accessible` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
版本要求：用于清晰辨识的设计版本。相邻重要元素除颜色差异外，还需有轮廓或明度差异；必要文字与底色对比充分，不凭色彩单独编码语义。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding-accessible.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页 · 均衡层次

ID: `ui-onboarding-balanced` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
版本要求：用于均衡层次的呈现。控制前中后景或主次信息的视觉权重，避免边缘装饰抢占注意力，保留全部关键对象、文字与关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding-balanced.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 引导页 · 干净收尾

ID: `ui-onboarding-clean` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：冥想应用的三张新手引导页面并排。
构图与视觉：每页一个抽象呼吸图形，下方标题“Pause”“Breathe”“Begin”。
必须遵守：三页组件和间距一致，按钮位置稳定。
版本要求：用于清理视觉瑕疵后的版本。重点消除不必要的重影、断裂边缘、融合物件和伪文字；不改变指定内容，不新增解释性元素。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-onboarding-clean.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页

ID: `ui-checkout` · 基础案例 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页 · 编辑精选

ID: `ui-checkout-editorial` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
版本要求：用于编辑精选展示。保留主要内容和媒介，删减非必要的背景装饰，让主视觉与次要细节的层级更鲜明。不要删除任务要求的文字、对象或图解关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout-editorial.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页 · 缩略图优先

ID: `ui-checkout-thumbnail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
版本要求：用于缩略图浏览。通过更清晰的轮廓、主体与背景明暗分离来提升识别度；弱化非必要微小纹理，保留所有必要文字和完整主体。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout-thumbnail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页 · 细节呈现

ID: `ui-checkout-detail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
版本要求：用于近距离查看。提升材质边缘、结构交界和局部细节的可辨性；沿用原构图和对象数量，不通过堆叠装饰来增加细节。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout-detail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页 · 克制留白

ID: `ui-checkout-quiet` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
版本要求：用于安静、克制的视觉呈现。减少背景纹理和非必要装饰，强化负空间；保留必要的标签、关系、物件与原有视觉媒介。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout-quiet.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页 · 演示使用

ID: `ui-checkout-presentation` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
版本要求：用于演示页面。确保远距离观看时主体层次仍然明确，必要文字清楚，信息区域之间有充足间距；不添加新的标题、数据或图例。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout-presentation.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页 · 印刷审阅

ID: `ui-checkout-print` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
版本要求：用于印刷前视觉审阅。避免暗部完全糊成一片与高光大面积纯白溢出，保留边缘和细节；保持既有色板。本图仍是栅格概念，不声称已满足印刷色彩或出血规格。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout-print.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页 · 清晰辨识

ID: `ui-checkout-accessible` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
版本要求：用于清晰辨识的设计版本。相邻重要元素除颜色差异外，还需有轮廓或明度差异；必要文字与底色对比充分，不凭色彩单独编码语义。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout-accessible.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页 · 均衡层次

ID: `ui-checkout-balanced` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
版本要求：用于均衡层次的呈现。控制前中后景或主次信息的视觉权重，避免边缘装饰抢占注意力，保留全部关键对象、文字与关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout-balanced.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 结算页 · 干净收尾

ID: `ui-checkout-clean` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构咖啡器具商店的移动端结算界面。
构图与视觉：商品摘要、配送信息占位块与付款按钮清晰分组。
必须遵守：不出现真实卡号或地址，按钮文字“Place order”。
版本要求：用于清理视觉瑕疵后的版本。重点消除不必要的重影、断裂边缘、融合物件和伪文字；不改变指定内容，不新增解释性元素。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-checkout-clean.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面

ID: `ui-calendar` · 基础案例 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面 · 编辑精选

ID: `ui-calendar-editorial` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
版本要求：用于编辑精选展示。保留主要内容和媒介，删减非必要的背景装饰，让主视觉与次要细节的层级更鲜明。不要删除任务要求的文字、对象或图解关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar-editorial.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面 · 缩略图优先

ID: `ui-calendar-thumbnail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
版本要求：用于缩略图浏览。通过更清晰的轮廓、主体与背景明暗分离来提升识别度；弱化非必要微小纹理，保留所有必要文字和完整主体。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar-thumbnail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面 · 细节呈现

ID: `ui-calendar-detail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
版本要求：用于近距离查看。提升材质边缘、结构交界和局部细节的可辨性；沿用原构图和对象数量，不通过堆叠装饰来增加细节。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar-detail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面 · 克制留白

ID: `ui-calendar-quiet` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
版本要求：用于安静、克制的视觉呈现。减少背景纹理和非必要装饰，强化负空间；保留必要的标签、关系、物件与原有视觉媒介。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar-quiet.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面 · 演示使用

ID: `ui-calendar-presentation` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
版本要求：用于演示页面。确保远距离观看时主体层次仍然明确，必要文字清楚，信息区域之间有充足间距；不添加新的标题、数据或图例。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar-presentation.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面 · 印刷审阅

ID: `ui-calendar-print` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
版本要求：用于印刷前视觉审阅。避免暗部完全糊成一片与高光大面积纯白溢出，保留边缘和细节；保持既有色板。本图仍是栅格概念，不声称已满足印刷色彩或出血规格。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar-print.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面 · 清晰辨识

ID: `ui-calendar-accessible` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
版本要求：用于清晰辨识的设计版本。相邻重要元素除颜色差异外，还需有轮廓或明度差异；必要文字与底色对比充分，不凭色彩单独编码语义。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar-accessible.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面 · 均衡层次

ID: `ui-calendar-balanced` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
版本要求：用于均衡层次的呈现。控制前中后景或主次信息的视觉权重，避免边缘装饰抢占注意力，保留全部关键对象、文字与关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar-balanced.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 日历界面 · 干净收尾

ID: `ui-calendar-clean` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：轻量日程应用周视图，三个不同颜色的日程块。
构图与视觉：浅色界面，左侧时间轴，当前日期标记清晰。
必须遵守：作为视觉概念，避免声称日期与星期对应已验证。
版本要求：用于清理视觉瑕疵后的版本。重点消除不必要的重影、断裂边缘、融合物件和伪文字；不改变指定内容，不新增解释性元素。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-calendar-clean.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏

ID: `ui-landing` · 基础案例 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏 · 编辑精选

ID: `ui-landing-editorial` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
版本要求：用于编辑精选展示。保留主要内容和媒介，删减非必要的背景装饰，让主视觉与次要细节的层级更鲜明。不要删除任务要求的文字、对象或图解关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing-editorial.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏 · 缩略图优先

ID: `ui-landing-thumbnail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
版本要求：用于缩略图浏览。通过更清晰的轮廓、主体与背景明暗分离来提升识别度；弱化非必要微小纹理，保留所有必要文字和完整主体。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing-thumbnail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏 · 细节呈现

ID: `ui-landing-detail` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
版本要求：用于近距离查看。提升材质边缘、结构交界和局部细节的可辨性；沿用原构图和对象数量，不通过堆叠装饰来增加细节。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing-detail.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏 · 克制留白

ID: `ui-landing-quiet` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
版本要求：用于安静、克制的视觉呈现。减少背景纹理和非必要装饰，强化负空间；保留必要的标签、关系、物件与原有视觉媒介。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing-quiet.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏 · 演示使用

ID: `ui-landing-presentation` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
版本要求：用于演示页面。确保远距离观看时主体层次仍然明确，必要文字清楚，信息区域之间有充足间距；不添加新的标题、数据或图例。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing-presentation.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏 · 印刷审阅

ID: `ui-landing-print` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
版本要求：用于印刷前视觉审阅。避免暗部完全糊成一片与高光大面积纯白溢出，保留边缘和细节；保持既有色板。本图仍是栅格概念，不声称已满足印刷色彩或出血规格。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing-print.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏 · 清晰辨识

ID: `ui-landing-accessible` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
版本要求：用于清晰辨识的设计版本。相邻重要元素除颜色差异外，还需有轮廓或明度差异；必要文字与底色对比充分，不凭色彩单独编码语义。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing-accessible.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏 · 均衡层次

ID: `ui-landing-balanced` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
版本要求：用于均衡层次的呈现。控制前中后景或主次信息的视觉权重，避免边缘装饰抢占注意力，保留全部关键对象、文字与关系。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing-balanced.txt)

检查：信息层级；对齐；交互含义与概念边界。

## SaaS首屏 · 干净收尾

ID: `ui-landing-clean` · 任务侧重变体 · `generate` · 建议尺寸 `1536x1024`
参考图：0 张（最低） · 状态：`prompt-only`

```text
任务：创作一张完整图像。
主体与场景：虚构阅读笔记工具的桌面网站首屏。
构图与视觉：左侧标题“Read. Remember.”和按钮“Get started”，右侧卡片式笔记预览。
必须遵守：层级简洁，不编造客户标志和用户增长数字。
版本要求：用于清理视觉瑕疵后的版本。重点消除不必要的重影、断裂边缘、融合物件和伪文字；不改变指定内容，不新增解释性元素。
交付检查：信息层级、对齐、交互含义与概念边界。不添加未要求的水印或署名。
```

[下载文本](../prompts/ui-landing-clean.txt)

检查：信息层级；对齐；交互含义与概念边界。

## 阅读应用

ID: `app-mockup` · 基础案例 · `generate` · 建议尺寸 `1024x1536`
参考图：0 张（最低） · 状态：`prompt-only`

```text
Design one polished mobile reading-app screen on an ivory background, straight-on without device perspective. A small header reads 'Library'. Three large book-cover blocks appear below, followed by a 'Continue reading' card and a bottom navigation bar labelled 'Home', 'Library', 'Notes'. Warm charcoal typography, muted olive accents, consistent spacing, legible hierarchy. This is a visual interface concept, not a functional app.
```

[下载文本](../prompts/app-mockup.txt)

检查：主体完整；文字准确；构图清晰；编辑保真。
