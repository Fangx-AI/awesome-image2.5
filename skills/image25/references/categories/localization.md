# 本地化与批量一致性 · Localization & consistency

[返回总览](../gallery.md)

所有条目均未经过指定模型出图验证。以“变体”标记的条目共享同一基础场景。

## 招牌翻译

ID: `localization-sign-translate` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate.txt)

检查：原文与目标文；版式一致；系列元素。

## 招牌翻译 · 边缘精修

ID: `localization-sign-translate-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate-edge.txt)

检查：原文与目标文；版式一致；系列元素。

## 招牌翻译 · 光影衔接

ID: `localization-sign-translate-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate-light.txt)

检查：原文与目标文；版式一致；系列元素。

## 招牌翻译 · 纹理衔接

ID: `localization-sign-translate-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate-texture.txt)

检查：原文与目标文；版式一致；系列元素。

## 招牌翻译 · 透视对齐

ID: `localization-sign-translate-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate-perspective.txt)

检查：原文与目标文；版式一致；系列元素。

## 招牌翻译 · 细节保留

ID: `localization-sign-translate-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate-identity.txt)

检查：原文与目标文；版式一致；系列元素。

## 招牌翻译 · 色彩匹配

ID: `localization-sign-translate-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate-color.txt)

检查：原文与目标文；版式一致；系列元素。

## 招牌翻译 · 文字保护

ID: `localization-sign-translate-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate-text.txt)

检查：原文与目标文；版式一致；系列元素。

## 招牌翻译 · 最小改动

ID: `localization-sign-translate-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate-minimal.txt)

检查：原文与目标文；版式一致；系列元素。

## 招牌翻译 · 成品审阅

ID: `localization-sign-translate-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，把主招牌英文替换为“山间咖啡”。
构图与视觉：目标文字适应原招牌透视、材质、光照与排版空间。
必须遵守：只改主招牌，其余文字和场景完全保留。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-sign-translate-final.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题

ID: `localization-poster-title` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题 · 边缘精修

ID: `localization-poster-title-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title-edge.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题 · 光影衔接

ID: `localization-poster-title-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title-light.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题 · 纹理衔接

ID: `localization-poster-title-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title-texture.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题 · 透视对齐

ID: `localization-poster-title-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title-perspective.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题 · 细节保留

ID: `localization-poster-title-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title-identity.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题 · 色彩匹配

ID: `localization-poster-title-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title-color.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题 · 文字保护

ID: `localization-poster-title-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title-text.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题 · 最小改动

ID: `localization-poster-title-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title-minimal.txt)

检查：原文与目标文；版式一致；系列元素。

## 海报改标题 · 成品审阅

ID: `localization-poster-title-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1为目标，将海报主标题替换为“看见日常”。
构图与视觉：保持原有字体粗细、对齐、行距和视觉层级。
必须遵守：不修改副标题、图像、日期和背景。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-poster-title-final.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体

ID: `localization-package-variant` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体 · 边缘精修

ID: `localization-package-variant-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant-edge.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体 · 光影衔接

ID: `localization-package-variant-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant-light.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体 · 纹理衔接

ID: `localization-package-variant-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant-texture.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体 · 透视对齐

ID: `localization-package-variant-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant-perspective.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体 · 细节保留

ID: `localization-package-variant-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant-identity.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体 · 色彩匹配

ID: `localization-package-variant-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant-color.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体 · 文字保护

ID: `localization-package-variant-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant-text.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体 · 最小改动

ID: `localization-package-variant-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant-minimal.txt)

检查：原文与目标文；版式一致；系列元素。

## 包装系列变体 · 成品审阅

ID: `localization-package-variant-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1包装为基础制作新的鼠尾草绿配色版本。
构图与视觉：保持标志、标签排版、产品结构和拍摄角度一致。
必须遵守：仅改指定主色，不改变文字内容。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-package-variant-final.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体

ID: `localization-avatar-season` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体 · 边缘精修

ID: `localization-avatar-season-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season-edge.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体 · 光影衔接

ID: `localization-avatar-season-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season-light.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体 · 纹理衔接

ID: `localization-avatar-season-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season-texture.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体 · 透视对齐

ID: `localization-avatar-season-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season-perspective.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体 · 细节保留

ID: `localization-avatar-season-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season-identity.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体 · 色彩匹配

ID: `localization-avatar-season-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season-color.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体 · 文字保护

ID: `localization-avatar-season-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season-text.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体 · 最小改动

ID: `localization-avatar-season-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season-minimal.txt)

检查：原文与目标文；版式一致；系列元素。

## 头像节日变体 · 成品审阅

ID: `localization-avatar-season-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：以图1原创吉祥物为基础，增加一顶简洁冬帽。
构图与视觉：保持头像裁切、主角脸型、眼睛和原有背景。
必须遵守：帽子不遮住眼睛，不增加标题或改变身份。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-avatar-season-final.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续

ID: `localization-thumbnail-series` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续 · 边缘精修

ID: `localization-thumbnail-series-edge` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
版本要求：重点检查修改区域的轮廓、细线和发丝交界；消除可见贴图边缘，不改变区域以外内容。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series-edge.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续 · 光影衔接

ID: `localization-thumbnail-series-light` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
版本要求：重点让新内容的光源方向、明暗、反射和接触阴影与原图一致；不要重新照亮整个画面。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series-light.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续 · 纹理衔接

ID: `localization-thumbnail-series-texture` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
版本要求：重点匹配局部颗粒、清晰度、材质纹理尺度和压缩观感；不把未修改区域整体磨皮或锐化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series-texture.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续 · 透视对齐

ID: `localization-thumbnail-series-perspective` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
版本要求：重点检查修改对象的透视、朝向、尺度和遮挡关系；相机位置和原有构图不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series-perspective.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续 · 细节保留

ID: `localization-thumbnail-series-identity` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
版本要求：重点保留原图中身份相关特征、独特物件标记和细节；只执行明确指定的改动，不额外美化。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series-identity.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续 · 色彩匹配

ID: `localization-thumbnail-series-color` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
版本要求：重点匹配修改区域与周围环境的白平衡、饱和度和色彩反射；仅当基础任务指定改色时改变目标颜色。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series-color.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续 · 文字保护

ID: `localization-thumbnail-series-text` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
版本要求：逐处保护原有文字、符号和数字；若基础任务要求换字，仅替换指定文字，其他文字逐字不变。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series-text.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续 · 最小改动

ID: `localization-thumbnail-series-minimal` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
版本要求：把修改范围收紧到完成基础任务所必需的区域，最大限度保留其余画面；不要重构背景。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series-minimal.txt)

检查：原文与目标文；版式一致；系列元素。

## 封面系列延续 · 成品审阅

ID: `localization-thumbnail-series-final` · 任务侧重变体 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
任务：编辑提供的参考图片。
主体与场景：图1是系列封面参考，制作同系列的新画面，把中央书本替换为笔记本。
构图与视觉：保留背景、配色、留白、构图重心和阴影风格。
必须遵守：不修改已有标题文字，只替换指定主体。
版本要求：在执行基础编辑后，重点检查错位、重影、残留边缘和遮挡破绽；修复这些局部瑕疵，其余内容保持原样。
交付检查：原文与目标文、版式一致、系列元素。检查非修改区域是否保持一致。
```

[下载文本](../prompts/localization-thumbnail-series-final.txt)

检查：原文与目标文；版式一致；系列元素。

## 中文标题替换

ID: `text-localize` · 基础案例 · `edit` · 建议尺寸 `auto`
参考图：1 张（最低） · 状态：`prompt-only`

```text
Edit image 1: replace the main headline with the exact Chinese text “慢下来，看见生活”. Match the original headline's placement, perspective, color and visual weight. Preserve all other text, objects, colors, background texture and image dimensions. Do not add additional characters or redesign the layout.
```

[下载文本](../prompts/text-localize.txt)

检查：主体完整；文字准确；构图清晰；编辑保真。
